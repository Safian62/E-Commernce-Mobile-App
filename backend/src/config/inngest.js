import { Inngest } from "inngest";

export const inngest = new Inngest({ id: "ecommerce-app" });

// No Clerk-related functions anymore; you can add your own background jobs here if needed.
export const functions = [];
